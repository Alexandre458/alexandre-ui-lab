# Bootstrap HTTP para validação ACME. Substituído pelo vhost final após emissão TLS.
server {
    listen 80;
    listen [::]:80;
    server_name ui.alexandresilva.dev;
    include /etc/nginx/conf.d/include/letsencrypt-acme-challenge.conf;
    location / { return 404; }
}
